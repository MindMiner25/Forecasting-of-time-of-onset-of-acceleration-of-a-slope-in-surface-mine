int n = read.getRows();
		
		// // 1 day = 1440 min
		// // 1 day = 1440/15 = 96 rows.

		// int window_size = 96;
        // preChecker stages;
		// for (int i = 0; i + window_size < n; i++)
		// {
		// 	if (stages.stageI (i, i + window_size)) {
		// 		if (stages.stageII (i, i + window_size)) {
		// 			if (stages.stageIII (i, i + window_size)) {

		// 			}
		// 		}
		// 	}
		// }